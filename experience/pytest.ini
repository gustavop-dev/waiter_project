[pytest]
DJANGO_SETTINGS_MODULE = experience_project.settings
python_files = test_*.py
testpaths = experience_app/tests
norecursedirs = venv
addopts = -ra -m "not contract"
markers =
    contract: corre contra el Odoo real del compose; falla si no responde
