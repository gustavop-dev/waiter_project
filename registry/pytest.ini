[pytest]
DJANGO_SETTINGS_MODULE = registry_project.settings
python_files = test_*.py
testpaths = registry_app/tests
norecursedirs = venv
addopts = -ra -m "not contract"
markers =
    contract: corre contra el Odoo real del compose; falla si no responde
