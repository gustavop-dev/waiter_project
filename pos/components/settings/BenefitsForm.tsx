'use client'
import {useEffect,useState} from 'react'
import {Button} from '@/components/ui/Button'
import {TextInput} from '@/components/ui/Field'
import {benefitsSettings,type Coupon,type PointsSettings} from '@/lib/services/benefits'
const empty:Coupon={name:'',code:'',percent:10,minimum:0,start:'',end:'',active:true}
export function BenefitsForm({configId}:{configId:number}) {
 const [coupons,setCoupons]=useState<Coupon[]>([]),[draft,setDraft]=useState<Coupon>(empty),[points,setPoints]=useState<PointsSettings|null>(null)
 const [busy,setBusy]=useState(false),[error,setError]=useState(''),[saved,setSaved]=useState('')
 useEffect(()=>{let active=true;benefitsSettings(configId).then(r=>{if(active){setCoupons(r.coupons);setPoints(r.loyalty)}}).catch(e=>{if(active)setError(e.message)});return()=>{active=false}},[configId])
 const save=async(kind:'coupon'|'points')=>{setBusy(true);setError('');setSaved('');try{const r=await benefitsSettings(configId,kind==='coupon'?draft:undefined,kind==='points'&&points?points:undefined);setCoupons(r.coupons);setPoints(r.loyalty);if(kind==='coupon')setDraft(empty);setSaved('Cambios guardados. Ya están disponibles en el menú y el POS.')}catch(e){setError(e instanceof Error?e.message:'No se pudo guardar')}finally{setBusy(false)}}
 return <div className="flex flex-col gap-5 max-w-3xl">{error&&<p role="alert" className="text-danger">{error}</p>}{saved&&<p role="status">{saved}</p>}<section className="rounded-md border border-border p-5 flex flex-col gap-4"><h3 className="font-semibold">Cupones de descuento</h3><p className="text-sm text-soft">Códigos reutilizables sobre el consumo de quien los aplica. Sustituyen el descuento de primera compra; no se suman a él.</p>{coupons.map(c=><button key={c.id} className="flex justify-between border border-border rounded-md p-3 text-left" onClick={()=>{setDraft(c);setSaved('')}}><span>{c.code} · {c.name}</span><span>{c.percent}% · {c.active?'Activo':'Inactivo'} · Editar</span></button>)}<form onSubmit={e=>{e.preventDefault();void save('coupon')}} className="flex flex-col gap-4"><h4>{draft.id?'Editar cupón':'Crear cupón'}</h4><div className="grid grid-cols-2 gap-4"><TextInput required label="Nombre" value={draft.name} onChange={e=>setDraft({...draft,name:e.target.value})}/><TextInput required label="Código" maxLength={32} minLength={3} pattern="[A-Za-z0-9_-]{3,32}" value={draft.code} onChange={e=>setDraft({...draft,code:e.target.value.toUpperCase()})}/><TextInput label="Descuento (%)" required type="number" min="0.01" max="100" step="0.01" value={draft.percent} onChange={e=>setDraft({...draft,percent:Number(e.target.value)})}/><TextInput label="Compra mínima (con impuestos)" type="number" min="0" step="0.01" required value={draft.minimum} onChange={e=>setDraft({...draft,minimum:Number(e.target.value)})}/><TextInput label="Desde (opcional)" type="date" value={draft.start} onChange={e=>setDraft({...draft,start:e.target.value})}/><TextInput label="Hasta (opcional)" type="date" value={draft.end} onChange={e=>setDraft({...draft,end:e.target.value})}/></div><label className="flex gap-2"><input type="checkbox" checked={draft.active} onChange={e=>setDraft({...draft,active:e.target.checked})}/>Cupón activo</label><div className="flex gap-3"><Button type="submit" disabled={busy}>Guardar cupón</Button>{draft.id&&<Button type="button" variant="secondary" onClick={()=>setDraft(empty)}>Cancelar edición</Button>}</div></form></section><section className="rounded-md border border-border p-5 flex flex-col gap-4"><h3 className="font-semibold">Puntos de fidelización</h3>{points?<form className="flex flex-col gap-4" onSubmit={e=>{e.preventDefault();void save('points')}}><p>{points.name}</p><p className="text-sm text-soft">Se acreditan cuando el POS cobra el pedido. Las devoluciones descuentan los puntos correspondientes.</p><TextInput label="Consumo para ganar 1 punto" type="number" required min="0.01" step="0.01" value={points.spendPerPoint} onChange={e=>setPoints({...points,spendPerPoint:Number(e.target.value)})}/><TextInput label="Valor de descuento por punto" type="number" required min="0.01" step="0.01" value={points.valuePerPoint} onChange={e=>setPoints({...points,valuePerPoint:Number(e.target.value)})}/><TextInput label="Puntos mínimos para canjear" type="number" required min="1" step="1" value={points.minimumPoints} onChange={e=>setPoints({...points,minimumPoints:Number(e.target.value)})}/><Button type="submit" disabled={busy}>Guardar puntos</Button></form>:<p>No hay un programa de puntos activo para este POS.</p>}</section></div>
}
